name: 'urban_mobility'
version: '1.0.0'
config-version: 2
profile: 'urban_mobility'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  urban_mobility:
    staging:
      +materialized: view
    marts:
      +materialized: table